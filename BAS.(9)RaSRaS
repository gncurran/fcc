function repeat(str, num) {
  if (num < 0) return "";
  var result = "";
  while (num > 1) {
    if (num = 1) result += str;
    num > 1, str += str;
  }
  return result + str;
}

repeat("abc", 3, "");
