function fearNotLetter(str) {

  for (var i in str) {
    if (str.charCodeAt(parseInt(i)+1)-str.charCodeAt(i)>1){
      return String.fromCharCode(str.charCodeAt(i)+1);
      }
    if (parseInt(i)==str.length-2) {break;}
    }
   return undefined;
  }

fearNotLetter("abce");
